{
  "name": "",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "init": "git flow init -d && npm install -g npm-check-updates && npm install",
    "update": "rm -rf node_modules && ncu -u && npm install",
    "preinstall": "npm cache clean"
  },
  "dependencies": {
    "normalize.css": "^3.0.3",

    "babel": "^5.8.23",
    "babel-runtime": "^5.8.20",

    "react": "0.14.0-beta3",
    "react-dom": "0.14.0-beta3",
    "classnames": "^2.1.3"
  },
  "devDependencies": {
    "webpack": "^1.12.1",
    "webpack-dev-server": "gsklee/webpack-dev-server"
  },
  "engines": {
    "iojs": ">=3.3.0"
  },
  "babel": {
    "stage": 0,
    "loose": "all",
    "optional": [
      "runtime",
      "minification.constantFolding"
    ]
  }
}
